import { TipoUsuarioPipe } from './tipo-usuario.pipe';

describe('TipoUsuarioPipe', () => {
  it('create an instance', () => {
    const pipe = new TipoUsuarioPipe();
    expect(pipe).toBeTruthy();
  });
});
