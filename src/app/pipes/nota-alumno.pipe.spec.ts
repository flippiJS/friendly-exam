import { NotaAlumnoPipe } from './nota-alumno.pipe';

describe('NotaAlumnoPipe', () => {
  it('create an instance', () => {
    const pipe = new NotaAlumnoPipe();
    expect(pipe).toBeTruthy();
  });
});
