import { CupoDirective } from './cupo.directive';

describe('CupoDirective', () => {
  it('should create an instance', () => {
    const directive = new CupoDirective();
    expect(directive).toBeTruthy();
  });
});
